
// This file is no longer used as the backend has been removed.
// It is kept to prevent import errors in case it's referenced,
// but it does not establish a database connection.

const clientPromise = Promise.resolve({});

export default clientPromise;
